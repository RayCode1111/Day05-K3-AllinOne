# Gemini API: giữ key thật trong .env, tuyệt đối không commit.
GOOGLE_API_KEY=
GEMINI_MODEL=gemini-3.6-flash

# Chế độ app
APP_ENV=development
APP_DATA_MODE=live

# Tuỳ chọn: cổng mặc định của Streamlit được cấu hình trong .streamlit/config.toml
# STREAMLIT_SERVER_PORT=8501
